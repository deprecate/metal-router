{
  "name": "metal-router",
  "version": "3.0.3",
  "description": "Routing solution to link URLs to Metal.js components using HTML5 History API.",
  "license": "BSD",
  "repository": "metal/metal-router",
  "engines": {
    "node": ">=0.12.0",
    "npm": ">=3.0.0"
  },
  "jsnext:main": "src/all/router.js",
  "main": "lib/all/router.js",
  "files": [
    "build",
    "lib",
    "src"
  ],
  "scripts": {
    "build:demo": "gulp soy && metal build -s demos/src/**/*.js -d demos/build --soySrc demos/src/**/*.soy --soyDest demos/src --soyDeps src/RouterSoy.soy",
    "compile": "babel --presets metal -d lib/ src/",
    "prepublish": "gulp && npm run compile",
    "test": "gulp test"
  },
  "keywords": [
    "metal"
  ],
  "dependencies": {
    "bootstrap": "^3.3.6",
    "metal-promise": "^2.0.0",
    "metal-soy": "^2.0.0",
    "senna": "^2.0.0"
  },
  "devDependencies": {
    "babel-cli": "^6.4.5",
    "babel-preset-metal": "^4.0.0",
    "gulp": "^3.8.11",
    "gulp-metal": "^1.7.1",
    "metal-ajax": "^2.0.3",
    "metal-cli": "^2.0.0",
    "metal-karma-config": "^2.1.0",
    "run-sequence": "^1.2.2"
  }
}
